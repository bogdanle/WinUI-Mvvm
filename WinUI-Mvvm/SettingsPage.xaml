<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinUI_Mvvm.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI_Mvvm"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Orientation="Vertical" Margin="5">
            <TextBlock Text="Choose a theme" Margin="0,0,0,10" />
            <RadioButton Content="Light" GroupName="Theme" IsChecked="{x:Bind ViewModel.IsLightTheme, Mode=TwoWay}" />
            <RadioButton Content="Dark" GroupName="Theme" IsChecked="{x:Bind ViewModel.IsDarkTheme, Mode=TwoWay}" />
            <RadioButton Content="Default" GroupName="Theme" IsChecked="{x:Bind ViewModel.IsDefaultTheme, Mode=TwoWay}" />
        </StackPanel>
    </Grid>
</Page>
